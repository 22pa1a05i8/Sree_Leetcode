class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        c=0
        for i in sentences:
            l=i.split(" ")
            if(c<len(l)):
                c=len(l)
        return c
            
